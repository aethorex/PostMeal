<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="style.css" />
  </head>
  <body>
    <header class="addMeal">
      <div class="divLogin">
        <% if (user) { %>
        <h1><a href="/logout"><%= user.name %></a></h1>
        <% } else { %>
        <h1><a href="/login">Login</a></h1>
        <% } %>
      </div>
      <nav>
        <ul>
          <li><a href="/">Home</a></li>
          <li><a href="/add">Add Meal</a></li>
          <li><a href="/myRes">My Recipes</a></li>
        </ul>
      </nav>
    </header>

    <div class="divAddMeal">
      <% if (user) { %>
      <form class="login" action="/add" method="POST">
        <h1>Add Meal</h1>
        <hr color="white" width="100%" />
        <br />
        <input type="text" placeholder="Recipe Title" name="title" />
        <textarea name="meal">Description</textarea>

        <button type="submit">Post</button>
      </form>
      <% } else { %>
      <h1>You must be logged-in, in order to add a meal.</h1>
      <% } %>
    </div>
  </body>
</html>
